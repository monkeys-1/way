# ملف التخطيط الشامل للتطبيق Flutter (نسخة موحّدة وصارمة)

> **قاعدة تنفيذية:** هذا هو الملف المرجعي الواحد للتخطيط. لا يتم اعتماد أي قرار/تغيير إلا إذا تم توثيقه هنا.

## 1) نطاق هذا الملف (بدون هلوسة)
- تم بناء هذا الملف فقط من المحتوى الموجود في المستودع:
  - `/home/runner/work/way/way/Product Documentation Suite`
  - `/home/runner/work/way/way/Design Layer — Information Architecture & Wireframes`
  - `/home/runner/work/way/way/Engineering Layer — System Architecture`
  - `/home/runner/work/way/way/README.md`
- لا توجد إضافة معلومات خارجية غير مذكورة في الملفات أعلاه.

## 2) الوضع الحالي للمستودع (ما حدث فعليًا)
- المستودع الحالي وثائقي بالأساس، ولا يحتوي حاليًا على ملفات Flutter تنفيذية (لا يوجد `pubspec.yaml`).
- يوجد تكرار وثائقي: ملفا **Product Documentation Suite** و **Design Layer — Information Architecture & Wireframes** متطابقان بالمحتوى (حسب التحقق المحلي).
- يوجد مرجع هندسي منفصل يغطي المعمارية الكاملة والتقنيات والأمن وخطة CI/CD.

## 3) الأساس التقني المعتمد للتطبيق Flutter
(المصدر: Engineering Layer — System Architecture، قسم 2.1)
- Flutter 3.x + Dart
- إدارة الحالة: BLoC + Cubit
- التنقل: GoRouter
- الشبكات: Dio + Retrofit
- WebSocket: socket_io_client
- التخزين المحلي: Hive + SharedPrefs
- الاختبارات: flutter_test + mockito + integration_test
- CI/CD للموبايل: Fastlane + GitHub Actions

## 4) تعليمات صارمة للتنفيذ
1. **المصدر الواحد للحقيقة:** هذا الملف هو المرجع الوحيد للخطة، والتغييرات، والمخاطر.
2. **عدم الدمج بدون توثيق:** أي تعديل بالكود أو المتطلبات يجب أن يسبق/يتبع مباشرة تحديث هذا الملف.
3. **منع الغموض:** كل بند يجب أن يكون قابلًا للقياس (وقت/نسبة/نتيجة متوقعة).
4. **حوكمة التغيير:** الالتزام بتصنيف التغيير:
   - Patch (x.x.1): تغيير بسيط لا يمس المعمارية أو العقود.
   - Minor (x.1.x): إضافة ميزة متوافقة خلفيًا.
   - Major (1.x.x): تغيير معماري/جوهري وقد يكسر التوافق.

## 5) تسليمات صارمة (Deliverables)
لا يعتبر أي عمل مكتملًا إلا بتحقق جميع العناصر:
- [ ] متطلبات الميزة موثقة
- [ ] تصميم التدفق/الشاشات موثق
- [ ] عقود API/البيانات محدثة
- [ ] اختبارات Unit/Integration/E2E مضافة ومارة
- [ ] تحقق RTL (العربية) مكتمل
- [ ] التحقق الأمني مكتمل
- [ ] تحديث هذا الملف بسجل التغييرات

## 6) اختبارات صارمة (Quality Gates)
(المصدر: Product Documentation Suite، Section 9 + Section 11)
- الحد الأدنى للتغطية: Unit Tests = 70%
- اختبار تكامل شامل للمسارات الحرجة
- اختبارات أمنية إلزامية (Injection / XSS / AuthZ)
- اختبارات أداء ضمن حدود NFR:
  - API P95 < 500ms
  - تحميل الموبايل < 2s
- Definition of Done إلزامي قبل الإغلاق (اختبارات + مراجعة + QA + أمان + توثيق)

## 7) المشاكل الحالية
- لا يوجد كود Flutter فعلي داخل المستودع حتى الآن، وبالتالي لا يمكن تشغيل `flutter test` حاليًا.
- المستندات موزعة ومكررة جزئيًا؛ لذلك تم توحيد التخطيط في هذا الملف لتقليل التشتت.

## 8) الثغرات والمخاطر الأمنية (ملخص تنفيذي)
(المصدر: Product Section 14 + Engineering Section 5.1)
- مخاطر رئيسية يجب تغطيتها دائمًا:
  - Broken Access Control
  - Injection (SQL/XSS)
  - Authentication failures
  - Security misconfiguration
- ضوابط إلزامية:
  - TLS 1.3
  - RBAC + التحقق من ملكية المورد
  - Rate limiting
  - عدم تسريب بيانات حساسة في السجلات
  - إدارة أسرار عبر Vault/Secrets Manager

## 9) الحلول المعتمدة
- اعتماد Flutter كخيار أساسي متعدد المنصات مع دعم RTL.
- اعتماد مسار CI/CD واضح: GitHub Actions + Fastlane للموبايل.
- اعتماد إطار أمني متعدد الطبقات (شبكة/مصادقة/تفويض/بيانات/تدقيق).

## 10) سجل التغييرات (لهذا الطلب)
- **2026-05-18**
  - إنشاء ملف موحّد واحد للتخطيط الصارم للتطبيق Flutter.
  - تجميع: التعليمات الصارمة + التسليمات الصارمة + الاختبارات الصارمة + التقنيات + الحلول + المشاكل + الثغرات.
  - توثيق الوضع الحالي للمستودع كما هو بدون افتراضات.

## 11) معايير القبول النهائية
- [ ] هذا الملف محدث بعد كل تغيير فعلي
- [ ] لا يوجد بند غير قابل للقياس
- [ ] كل قرار أمني له ضابط تنفيذي واضح
- [ ] لا يتم إغلاق أي مهمة قبل استيفاء Quality Gates

